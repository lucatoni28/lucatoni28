diff --git a/src/common/itemMaterial.ts b/src/common/itemMaterial.ts
index 6d892d7..f0e233d 100644
--- a/src/common/itemMaterial.ts
+++ b/src/common/itemMaterial.ts
@@ -1,9 +1,14 @@
 import { CustomMaterial, type Scene } from '../libs/babylon/exports';
 
 /**
- * Is used as mask for item options
- * low 4 bits - item lvl
- * 5th bit - is excellent
+ * Mặt nạ bit đưa vào shader:
+ *   bit 0–3  cấp cường hoá 0–15
+ *   bit 4    đồ Xuất sắc  (0x10)
+ *   bit 5    đồ Thần      (0x20)
+ *
+ * Bit 5 là MỚI. Bản gốc chỉ có hai trường đầu, và bit "Xuất sắc" tuy được đọc
+ * ra biến bIsExcellent nhưng KHÔNG dùng ở đâu cả — đồ Xuất sắc sáng y hệt đồ
+ * thường. Giờ cả hai đều có lớp phủ riêng, xem phần "lớp phủ phẩm cấp" dưới.
  */
 const ITEM_OPTIONS_UNIFORM_NAME = `itemOptions`;
 
@@ -38,7 +43,7 @@ export function createItemMaterial(scene: Scene) {
     int iItemOptions = int(${ITEM_OPTIONS_UNIFORM_NAME});
     int iItemLvl = iItemOptions & 0x0F;
     bool bIsExcellent = (iItemOptions & 0x10) != 0;
-    vec3 vPartColor = vec3(1.0);
+    bool bIsDivine = (iItemOptions & 0x20) != 0;
 
     float wave = float(int(time * 1000.0) % 10000) * 0.0001;
 
@@ -78,9 +83,80 @@ export function createItemMaterial(scene: Scene) {
 
       color.rgb = color.rgb * 0.8 + color.rgb * vec3(1.0,0.9,0.0) + texColor.rgb * vec3(.7,.6,.5) * .3;
     }
-    else{
+    else { // 10 trở lên — TRẢ VỀ ĐÚNG MỘT NHÁNH NHƯ BẢN GỐC
+      /* Để ý: MỌI số hạng đều nhân với color.rgb, tức nhân với chính vân của
+         món đồ. Nhờ thế vân vẫn nhìn rõ, món chỉ lấp lánh thêm.
+
+         Bản trước tôi tách riêng +15 rồi CỘNG THẲNG một màu trắng ngà
+         (vec3(1.0,0.97,0.92) * n * pulse * 1.6). Cộng thẳng thì không ăn theo
+         vân nữa — bề mặt bị xoá trắng, nhân vật mặc đủ bộ thành một cục sáng
+         vàng. Bỏ hẳn nhánh ấy. */
       color.rgb = color.rgb * 1.4 + color.rgb * 2.0 * noise2(view.xz + time);
     }
+
+    /* ══════════ ĐỒ XUẤT SẮC / ĐỒ THẦN ══════════
+       Ba lớp, đúng thứ client MU thật cho ra: màu lên tươi, ánh trắng thuỷ
+       tinh lấp lánh, và một dải tím hồng chạy vòng quanh thân.
+
+       Bản gốc afrokick đọc cờ bIsExcellent ra biến rồi BỎ KHÔNG DÙNG, nên đồ
+       Xuất sắc sáng y hệt đồ thường. Đây là chỗ lắp nó vào.
+
+       Nguyên tắc xuyên suốt: KHÔNG phủ màu đè lên bề mặt. Lớp 1 kéo chính vân
+       gốc lên, lớp 2 và 3 chỉ cộng ở những chỗ hẹp (đỉnh nhiễu, dải mỏng), nên
+       vân model vẫn nhìn rõ. Bản trước tôi cộng thẳng một màu ra cả mặt, thế
+       là nhân vật thành cục sáng — không lặp lại. */
+    if (bIsExcellent || bIsDivine) {
+      /* ── CƠ CHẾ: LẤY ĐÚNG CỦA BẢN GỐC ──
+         Nhánh +7..+9 ở trên làm thế này:
+
+           uvOffset = vec2(n_first, n_second);   // trượt theo pháp tuyến + thời gian
+           texColor = texture2D(diffuseSampler, vDiffuseUV + uvOffset);
+           color.rgb = color.rgb * 0.8 + texColor.rgb * color.rgb * .9;
+
+         Tức là lấy mẫu CHÍNH VÂN của món đồ tại một toạ độ UV đang trượt, rồi
+         trộn ngược lên chính nó. Vệt sáng chạy chính là vân món đồ trượt trên
+         bản thân nó. Đây là cách MU làm, và dùng lại đúng cơ chế ấy thì hiệu
+         ứng ăn nhập với vân từng món, không phải lớp sơn phủ bên ngoài.
+
+         Trượt nhanh hơn nhánh +7..+9 (hệ số 1,5 và 3,0 thay vì 1,0 và 2,0)
+         để đồ Xuất sắc "sống" hơn đồ thường cùng cấp. */
+      float e_second = normalW.z * 0.5 + wave * 1.5;
+      float e_first  = normalW.y * 0.5 + wave * 3.0;
+      vec4 excTex = texture2D(diffuseSampler, vDiffuseUV + vec2(e_first, e_second));
+
+      /* ── 1. LÊN MÀU TƯƠI ──
+         Kéo bão hoà của chính vân gốc chứ không tô màu mới: giáp Rồng vốn đỏ
+         nâu sẫm sẽ bật thành đỏ tươi, giáp bạc vẫn ra bạc. Trộn ngược qua mức
+         xám với hệ số > 1 là cách tăng bão hoà mà không đụng sắc độ. */
+      float xam = dot(color.rgb, vec3(0.299, 0.587, 0.114));
+      color.rgb = mix(vec3(xam), color.rgb, 1.45);
+
+      /* ── 2. ÁNH TRẮNG THUỶ TINH LẤP LÁNH ──
+         Lấy kênh sáng nhất của mẫu đang trượt rồi nâng luỹ thừa 3: chỗ vân
+         vốn sáng thì bật hẳn lên trắng, chỗ tối gần như không đổi. Ra đúng
+         kiểu đốm chói trượt trên mặt thuỷ tinh — và vì nguồn là vân thật của
+         món nên đốm bám theo hoạ tiết giáp, không rải đều vô hồn. */
+      float sang = pow(max(max(excTex.r, excTex.g), excTex.b), 3.0);
+      color.rgb += vec3(1.0, 0.98, 0.95) * sang * 0.85;
+
+      /* ── 3. DẢI TÍM HỒNG CHẠY VÒNG QUANH THÂN ──
+         Góc phương vị của pháp tuyến, pha quay theo thời gian: mặt nào đang
+         quay đúng hướng thì dải quét qua, nên nhìn như dải sáng đi vòng quanh
+         người. Luỹ thừa 10 cho dải MỎNG, không loang cả mặt. Nhân thêm mẫu
+         trượt nên dải cũng gợn theo vân chứ không phải vạch sơn phẳng. */
+      float goc = atan(normalW.z, normalW.x);
+      float dai = pow(0.5 + 0.5 * cos(goc - time * 1.7), 10.0);
+      color.rgb += vec3(0.86, 0.32, 0.95) * dai * (0.5 + 0.5 * excTex.r) * 1.6;
+
+      /* Đồ Thần: dải ngả tím đậm hơn, để phân biệt với đồ Xuất sắc thường mà
+         không đổi ba lớp trên. Cùng màu chữ tên món trong bảng chú giải —
+         xem itemRank() bên src/offline/itemStats.ts. */
+      if (bIsDivine) {
+        vec3 V = normalize(vEyePosition.xyz - vPositionW);
+        float rim = pow(1.0 - abs(dot(normalize(vNormalW), V)), 3.0);
+        color.rgb += vec3(0.62, 0.35, 1.0) * rim * 0.45;
+      }
+    }
 `);
 
   let time = 0;
@@ -105,6 +181,10 @@ export function createItemMaterial(scene: Scene) {
         itemOptions |= 0x10;
       }
 
+      if (mesh.metadata?.isDivine) {
+        itemOptions |= 0x20;
+      }
+
       effect.setFloat(ITEM_OPTIONS_UNIFORM_NAME, itemOptions);
 
       effect.setTexture('diffuseSampler', mesh.metadata!.diffuseTexture);
diff --git a/src/common/modelLoader.ts b/src/common/modelLoader.ts
index 5eb2e84..15fdc3a 100644
--- a/src/common/modelLoader.ts
+++ b/src/common/modelLoader.ts
@@ -117,6 +117,7 @@ export async function loadGLTF(filePath: string, world: World) {
             mesh.metadata ??= {};
             mesh.metadata.itemLvl = 0;
             mesh.metadata.isExcellent = false;
+            mesh.metadata.isDivine = false;
             mesh.metadata.timeOffset = 0;
             mesh.alwaysSelectAsActiveMesh = true;
             mesh.isPickable = false;
diff --git a/src/common/modelObject.ts b/src/common/modelObject.ts
index 2628754..6320ae3 100644
--- a/src/common/modelObject.ts
+++ b/src/common/modelObject.ts
@@ -273,6 +273,29 @@ export class ModelObject {
   }
 
   Unload() {
+    /* GỠ HẲN MODEL KHỎI CẢNH, không chỉ hạ cờ.
+
+       Bản gốc thân hàm này đúng một dòng `this.Ready = false`. Cờ ấy chỉ nói
+       "chưa sẵn sàng", còn mesh thì vẫn nằm nguyên trong cảnh và vẫn được vẽ.
+       Hậu quả nhìn thấy được: cởi kiếm hay cởi cánh ra thì món đồ VẪN CÒN
+       trên người, mãi mãi — AppearanceSystem gọi Weapon1.Unload() /
+       Wings.Unload() rồi coi như xong, nhưng hình thì không đổi.
+
+       Gỡ mesh là đúng và cân với load(): chính load() cũng gọi
+       oldGltf.mesh.dispose() khi thay sang món khác. Mỗi lần loadGLTF dựng
+       một task riêng nên mesh không dùng chung với ai, xoá là an toàn.
+
+       KHÔNG dùng dispose(false, true) như hàm dispose() bên dưới: cờ thứ hai
+       xoá luôn vật liệu, mà vật liệu món đồ do createItemMaterial() dựng MỘT
+       lần rồi cả cảnh xài chung — xoá là hỏng hết đồ của mọi nhân vật khác.
+       Bỏ trống tham số, giống hệt load(), thì chỉ mesh bị gỡ. */
+    if (this.gltf) {
+      this.gltf.mesh.dispose();
+      this.gltf.skeleton?.dispose();
+      this.gltf.animationGroups.forEach(group => group.dispose());
+      this.gltf = null;
+    }
+
     this.Ready = false;
   }
 
diff --git a/src/common/objects/enum.ts b/src/common/objects/enum.ts
index baaa25a..7d0c44b 100644
--- a/src/common/objects/enum.ts
+++ b/src/common/objects/enum.ts
@@ -1043,4 +1043,7 @@ export const ItemGroups = {
   Pants: 9,
   Helm: 7,
   Armor: 8,
+  // Nhóm 12 gồm cả cánh lẫn ngọc kỹ năng (Orb) và cuộn — phân biệt bằng số
+  // món, xem items.json.
+  Wings: 12,
 } as const;
diff --git a/src/common/playerObject.ts b/src/common/playerObject.ts
index 51abd90..08c9af6 100644
--- a/src/common/playerObject.ts
+++ b/src/common/playerObject.ts
@@ -247,7 +247,8 @@ export class PlayerObject extends ModelObject {
     part: ModelObject,
     modelPath: string,
     itemLvl?: number,
-    isExcellent?: boolean
+    isExcellent?: boolean,
+    isDivine?: boolean
   ) {
     const gltf = await loadGLTF(dir + modelPath, Store.world!);
     part.load(gltf);
@@ -260,6 +261,7 @@ export class PlayerObject extends ModelObject {
       mesh.metadata ??= {};
       mesh.metadata.itemLvl = itemLvl ?? 0;
       mesh.metadata.isExcellent = isExcellent ?? false;
+      mesh.metadata.isDivine = isDivine ?? false;
     });
   }
 
diff --git a/src/common/resolveUrlToDataFolder.ts b/src/common/resolveUrlToDataFolder.ts
index ebd7ca7..e74e34f 100644
--- a/src/common/resolveUrlToDataFolder.ts
+++ b/src/common/resolveUrlToDataFolder.ts
@@ -1,3 +1,30 @@
+/**
+ * Bản dựng cho điện thoại nạp asset từ CDN GitHub hoặc từ thư mục phẳng img/,
+ * chứ không phải từ ./game-assets/ cạnh trang. Lớp giao diện đặt sẵn
+ * window.MU_ASSETS trước khi engine khởi động; có thì hỏi nó, không có thì giữ
+ * nguyên nếp cũ nên bản chạy trên máy tính không đổi gì.
+ *
+ * Trước đây chỗ này được vá bằng cách tìm-thay chuỗi trong file đã minify. Cách
+ * đó gãy ngay lần dựng lại engine đầu tiên vì minifier đổi tên hàm, nên giờ vá
+ * thẳng vào nguồn.
+ */
+export type MuAssets = {
+  game(relPath: string): string;
+  resolve(logicalPath: string): string;
+  readonly itemsBase: string;
+  readonly decoderBase: string;
+};
+
+declare global {
+  interface Window {
+    MU_ASSETS?: MuAssets;
+  }
+}
+
+export function muAssets(): MuAssets | undefined {
+  return typeof window !== 'undefined' ? window.MU_ASSETS : undefined;
+}
+
 export function resolveUrlToDataFolder(url: string): string {
   if (url.startsWith('http://') || url.startsWith('https://')) return url;
   if (url.startsWith('base64:')) return url;
@@ -7,5 +34,8 @@ export function resolveUrlToDataFolder(url: string): string {
     url = url.substring(1);
   }
 
+  const mu = muAssets();
+  if (mu) return mu.game(url);
+
   return `./game-assets/${url}`;
 }
diff --git a/src/ecs/createWorld.ts b/src/ecs/createWorld.ts
index d90f600..285915e 100644
--- a/src/ecs/createWorld.ts
+++ b/src/ecs/createWorld.ts
@@ -19,6 +19,8 @@ import { KeyboardInputSystem } from './systems/keyboardInputSystem';
 import { BackgroundMusicSystem } from './systems/backgroundMusicSystem';
 import { InteractiveAreaSystem } from './systems/interactiveAreaSystem';
 import { WalkSfxSystem } from './systems/walkSfxSystem';
+import { OfflineSpawnSystem } from '../offline/offlineSpawnSystem';
+import { OfflineCombatSystem } from '../offline/offlineCombatSystem';
 
 const factories: ISystemFactory[] = [
   ModelLoaderSystem,
@@ -31,6 +33,12 @@ const factories: ISystemFactory[] = [
   CalculateScreenPositionSystem,
   NetworkSystem,
   MoveAlongPathSystem,
+  // Hai hệ thống chơi đơn: tự tắt khi Store.isOffline sai, nên bản chơi mạng
+  // không bị ảnh hưởng gì. Đặt sau MoveAlongPathSystem để đọc được vị trí vừa
+  // cập nhật, trước AnimationSystem để animation đánh/chết kịp phát ngay khung
+  // này.
+  OfflineSpawnSystem,
+  OfflineCombatSystem,
   HighlightSystem,
   AnimationSystem,
   AppearanceSystem,
diff --git a/src/ecs/systems/animationSystem.ts b/src/ecs/systems/animationSystem.ts
index d58a63d..5e3d9cb 100644
--- a/src/ecs/systems/animationSystem.ts
+++ b/src/ecs/systems/animationSystem.ts
@@ -3,13 +3,16 @@ import { MonsterActionType, PlayerAction } from '../../common/objects/enum';
 import type { MUAttributeSystem } from '../../libs/attributeSystem';
 import type { ISystemFactory } from '../world';
 import type { PlayerObject } from '../../common/playerObject';
+import { TWFlags } from '../../common/terrain/consts';
+import { isFlagInBinaryMask } from '../../common/utils';
 
 export const AnimationSystem: ISystemFactory = world => {
   const playersQuery = world.with(
     'playerAnimation',
     'modelObject',
     'attributeSystem',
-    'movement'
+    'movement',
+    'transform'
   );
 
   const playerAnimatableQuery = world.with('modelObject', 'playerAnimation');
@@ -126,10 +129,43 @@ export const AnimationSystem: ISystemFactory = world => {
         playerAnimation,
         movement,
         attributeSystem,
+        transform,
       } of playersQuery) {
+        /* Cập nhật inSafeZone MỖI KHUNG.
+
+           Bản gốc chỉ đặt cờ này bên trong moveAlongPathSystem, và chỉ ở nhánh
+           "đang đi trên đường đi" — nên đứng yên là cờ không bao giờ đổi, và
+           lúc mới vào thế giới nó bằng 0 kể cả khi nhân vật đứng giữa làng.
+           Đo được: vừa vào Lorencia, đứng ở 135,131 mà inSafeZone vẫn false.
+           Cánh vì thế xoè ra ngay giữa làng. Đọc thẳng cờ địa hình ở đây thì
+           đứng hay đi đều đúng. */
+        const flag = world.getTerrainFlag(~~transform.pos.x, ~~transform.pos.z);
+        attributeSystem.setValue(
+          'inSafeZone',
+          isFlagInBinaryMask(flag, TWFlags.SafeZone) ? 1 : 0
+        );
+
+        /* Có cánh + ở NGOÀI vùng an toàn thì bay; trong vùng an toàn thì thu
+           cánh lại và đi bộ. Đúng nếp MU: về làng là hạ cánh.
+           calculateAnimation() bên dưới vốn đã xử lý đủ cả ba trường hợp
+           (trong vùng an toàn / bay / bơi) — nó chỉ thiếu người ĐẶT isFlying,
+           vì bản gốc đặt cứng bằng 0 ở logic.ts và offlineSpawnSystem.ts. */
+        attributeSystem.setValue(
+          'isFlying',
+          attributeSystem.isAboveZero('hasWings') &&
+            !attributeSystem.isAboveZero('inSafeZone')
+            ? 1
+            : 0
+        );
+
+        /* Đang vung đòn hoặc đang gục thì giữ nguyên, đừng tính lại — tính lại
+           là hoạt ảnh đánh bị ghi đè ngay khung sau và chẳng ai thấy gì.
+           Mốc PLAYER_ATTACK_FIST…PLAYER_ATTACK_END phủ mọi đòn thường, đòn
+           kỹ năng và Xoay Kiếm. */
         if (
-          playerAnimation.action === PlayerAction.PLAYER_ATTACK_FIST ||
-          playerAnimation.action === PlayerAction.PLAYER_DIE1
+          playerAnimation.action === PlayerAction.PLAYER_DIE1 ||
+          (playerAnimation.action >= PlayerAction.PLAYER_ATTACK_FIST &&
+            playerAnimation.action < PlayerAction.PLAYER_ATTACK_END)
         ) {
           continue;
         }
diff --git a/src/ecs/systems/appearanceSystem.ts b/src/ecs/systems/appearanceSystem.ts
index 4e4e936..a00c740 100644
--- a/src/ecs/systems/appearanceSystem.ts
+++ b/src/ecs/systems/appearanceSystem.ts
@@ -18,7 +18,8 @@ function loadPart(
     socket,
     item.szModelName,
     part.lvl,
-    part.isExcellent
+    part.isExcellent,
+    part.isDivine
   );
 
   return true;
@@ -52,6 +53,17 @@ export const AppearanceSystem: ISystemFactory = world => {
         loadPart(charAppearance.boots, playerObject, playerObject.Boots) ||
           playerObject.setDefaultBoots();
 
+        /* Cánh. Bản gốc để dòng nạp cánh trong playerObject.ts ở dạng chú
+           thích nên nhân vật không bao giờ có cánh; ổ cắm (ModelObject Wings,
+           nối vào xương 47) thì đã dựng sẵn từ trước. Nối nốt vào đây để cánh
+           đi theo đúng món đang mặc ở ô Wings, không phải hằng số. */
+        const coCanh = !!loadPart(
+          charAppearance.wings,
+          playerObject,
+          playerObject.Wings
+        );
+        if (!coCanh) playerObject.Wings.Unload();
+
         loadPart(charAppearance.leftHand, playerObject, playerObject.Weapon1) ||
           playerObject.Weapon1.Unload();
         loadPart(
@@ -61,6 +73,9 @@ export const AppearanceSystem: ISystemFactory = world => {
         ) || playerObject.Weapon2.Unload();
 
         if (attributeSystem) {
+          // AnimationSystem đọc cờ này mỗi khung để chọn bay hay đi bộ.
+          attributeSystem.setValue('hasWings', coCanh ? 1 : 0);
+
           if (charAppearance.leftHand) {
             const group = charAppearance.leftHand.group;
             const isSpear = group === 3;
diff --git a/src/ecs/world.ts b/src/ecs/world.ts
index b514c76..f3f44be 100644
--- a/src/ecs/world.ts
+++ b/src/ecs/world.ts
@@ -23,6 +23,19 @@ export type Item = {
   lvl?: number;
   isExcellent?: boolean;
   hasSkill?: boolean;
+  /**
+   * Ngọc đã khảm. Độ dài mảng = số lỗ đã mở (MU tối đa 5); mỗi ô là mã loại
+   * ngọc trong SOCKET_KINDS (src/offline/itemStats.ts) hoặc null nếu lỗ trống.
+   */
+  sockets?: (number | null)[];
+  /** Đồ Thần (Ancient/Divine) — tên hiện màu tím. */
+  isDivine?: boolean;
+  /**
+   * Mặt nạ bit của các dòng Xuất sắc. Sáu bit thấp, nghĩa của từng bit tuỳ
+   * theo món là vũ khí hay giáp — xem src/offline/excellent.ts.
+   * Chỉ có nghĩa khi isExcellent = true.
+   */
+  excOptions?: number;
 };
 
 export type Entity = Partial<{
@@ -72,6 +85,32 @@ export type Entity = Partial<{
     worldOffsetZ: number;
   };
   objectNameInWorld: string;
+
+  /* --- chỉ dùng ở chế độ chơi đơn, xem src/offline/ ---
+     Chỉ số quái lấy nguyên từ src/common/monsters.json (321 quái MU thật),
+     không đặt tay con số nào. */
+  monster: {
+    id: number;
+    name: string;
+    level: number;
+    hp: number;
+    maxHp: number;
+    minDmg: number;
+    maxDmg: number;
+    def: number;
+    attackSuccess: number;
+    exp: number;
+    moneyR: number;
+    itemR: number;
+    attackSpeed: number;
+    home: IVector2Like;
+    state: 'idle' | 'chase' | 'attack' | 'dead';
+    thinkAt: number;
+    attackAt: number;
+    diedAt: number;
+    aggroRange: number;
+    attackRange: number;
+  };
   charAppearance: {
     helm: Item | null;
     armor: Item | null;
diff --git a/src/libs/attributeSystem.ts b/src/libs/attributeSystem.ts
index 3f244f7..95f45ec 100644
--- a/src/libs/attributeSystem.ts
+++ b/src/libs/attributeSystem.ts
@@ -315,7 +315,10 @@ type GAttributesKeys =
   | 'playerNetClass'
   | 'totalMovementSpeed'
   | 'inSafeZone'
-  | 'isSpearEquipped';
+  | 'isSpearEquipped'
+  // Có cánh đang mặc hay không. AppearanceSystem đặt, AnimationSystem đọc để
+  // quyết định bay hay đi bộ.
+  | 'hasWings';
 
 type Stats = GAttributesKeys;
 type Others = GAttributesKeys;
diff --git a/src/libs/babylon/exports.ts b/src/libs/babylon/exports.ts
index 7f5ea3f..6a686e5 100644
--- a/src/libs/babylon/exports.ts
+++ b/src/libs/babylon/exports.ts
@@ -6,7 +6,12 @@ import '@babylonjs/core/Layers/effectLayerSceneComponent';
 
 const env = import.meta.env || {};
 
-let baseUrl = (env.BASE_URL || '') + 'js/';
+// Bản dựng cho điện thoại nạp decoder từ CDN hoặc img/ theo tên phẳng; lớp
+// giao diện đặt window.MU_ASSETS trước khi engine chạy. Không có thì giữ nếp
+// cũ, nên bản chạy trên máy tính không đổi gì.
+let baseUrl =
+  (typeof window !== 'undefined' && (window as any).MU_ASSETS?.decoderBase) ||
+  (env.BASE_URL || '') + 'js/';
 
 import { DracoCompression } from '@babylonjs/core/Meshes/Compression/dracoCompression';
 import { KhronosTextureContainer2 } from '@babylonjs/core/Misc/khronosTextureContainer2';
@@ -124,3 +129,9 @@ export { PBRBaseSimpleMaterial } from '@babylonjs/core/Materials/PBR/pbrBaseSimp
 
 export { UniversalCamera } from '@babylonjs/core/Cameras/universalCamera';
 export { HighlightLayer } from '@babylonjs/core/Layers/highlightLayer';
+export { GlowLayer } from '@babylonjs/core/Layers/glowLayer';
+
+/* ----------------------------------------------------------- lớp đồ hoạ thêm
+   Ký hiệu dưới đây phục vụ src/gfx (hậu kỳ làm nét).
+   Để ở đây vì bản dựng cho điện thoại lấy Babylon qua đúng file này. */
+export { SharpenPostProcess } from '@babylonjs/core/PostProcesses/sharpenPostProcess';
diff --git a/src/libs/eventBus/events.ts b/src/libs/eventBus/events.ts
index 79b2827..51ba043 100644
--- a/src/libs/eventBus/events.ts
+++ b/src/libs/eventBus/events.ts
@@ -28,4 +28,25 @@ export type Events = CSEvents &
     keyPressed: string;
     keyReleased: string;
     pageVisibilityChanged: boolean;
+
+    // --- chế độ chơi đơn ---
+    // Bản chơi đơn không có server nên toàn bộ chiến đấu tính ngay tại máy;
+    // những sự kiện dưới đây là cách lớp giao diện biết chuyện gì vừa xảy ra.
+    // x, y là toạ độ MÀN HÌNH (pixel) do CalculateScreenPositionSystem tính
+    // sẵn cho từng entity — lớp giao diện khỏi phải chiếu lại từ toạ độ 3D.
+    offlineHit: {
+      x: number;
+      y: number;
+      amount: number;
+      kind: 'dmg' | 'crit' | 'miss' | 'hurt';
+    };
+    offlineKill: { name: string; exp: number; zen: number };
+    offlineLevelUp: { level: number; points: number };
+    offlineStatSpent: { stat: string; points: number };
+    offlineItemMoved: { from: number; to: number };
+    offlineUpgrade: { slot: number; ok: boolean; lvl: number; cost: number };
+    offlineSocket: { slot: number; action: string; index: number; kind?: number };
+    offlineLoot: { name: string; zen: number };
+    offlineAttack: { slot: number };
+    offlinePlayerDied: {};
   };
diff --git a/src/main.tsx b/src/main.tsx
index 309ab53..279900d 100644
--- a/src/main.tsx
+++ b/src/main.tsx
@@ -12,6 +12,25 @@ import { createWorld } from './ecs/createWorld';
 import { ENUM_WORLD } from './common';
 import { EventBus } from './libs/eventBus';
 import { SoundsManager } from './libs/soundsManager';
+import { createGfx } from './gfx';
+import {
+  playerAttackRate,
+  playerDefense,
+  playerMaxDamage,
+  playerMinDamage,
+  gearDefense,
+  gearSet,
+} from './offline/combatFormulas';
+import { describeItem, itemRank, SOCKET_KINDS } from './offline/itemStats';
+import {
+  equipSlotsFor,
+  MAX_SOCKETS,
+  MAX_UPGRADE,
+  MOUNT_COST,
+  openSocketCost,
+  upgradeChance,
+  upgradeCost,
+} from './offline/itemActions';
 
 if (APP_STAGE === 'dev' || QA_ENABLED) {
   import('@babylonjs/core/Legacy/legacy');
@@ -98,6 +117,11 @@ Store.world = world;
 (window as any).__scene = scene;
 (window as any).__world = world;
 
+/* Bốn hiệu ứng đồ hoạ bật/tắt được — xem src/gfx. Lớp giao diện điện thoại
+   điều khiển qua biến này. */
+const gfx = createGfx(world);
+(window as any).__gfx = gfx;
+
 let lastTime = performance.now();
 engine.runRenderLoop(() => {
   const now = performance.now();
@@ -106,6 +130,7 @@ engine.runRenderLoop(() => {
   world.gameTime.TotalGameTime.TotalSeconds += deltaTime;
 
   updateSystems(deltaTime);
+  gfx.update(deltaTime);
 
   scene.render();
 
@@ -122,12 +147,52 @@ EventBus.on('requestWarp', ({ map, pos }) => {
   loadMapIntoScene(world, map, pos);
 });
 
+/* Đổi bản đồ là địa hình và vật thể dựng lại từ đầu, nên phải mắc lại bóng đổ
+   và ánh lửa vào lứa mới. */
+EventBus.on('warpCompleted', () => {
+  gfx.apply();
+});
+
 ReactDOM.createRoot(document.getElementById('root')!).render(
   <React.StrictMode>
     <App />
   </React.StrictMode>
 );
 
+/* Lớp giao diện ngoài (mu-mobile) đọc Store và phát sự kiện qua hai biến này.
+   Trước đây chúng được nối vào cuối file đã minify bằng cách dò tên biến —
+   minifier đổi tên mỗi lần dựng nên cách đó rất dễ gãy. Khai báo thẳng ở nguồn
+   thì tên không bao giờ trượt. */
+(window as any).__store = Store;
+(window as any).__eventBus = EventBus;
+
+/* Bốn số dẫn xuất của chế độ chơi đơn (sát thương, phòng thủ, tỉ lệ đánh trúng).
+   Phơi ra đây để bảng NHÂN VẬT hiển thị đúng con số engine đang dùng khi tính
+   sát thương, thay vì lớp giao diện tự chép lại công thức rồi lệch nhau. */
+(window as any).__combat = {
+  minDamage: playerMinDamage,
+  maxDamage: playerMaxDamage,
+  defense: playerDefense,
+  attackRate: playerAttackRate,
+  gearDefense,
+  gearSet,
+};
+
+/* Thông tin một món đồ và bảng giá cường hoá / khảm ngọc. Giao diện đọc qua
+   đây để bảng chú giải hiện đúng con số engine dùng khi đánh nhau. */
+(window as any).__itemStats = {
+  describe: describeItem,
+  rank: itemRank,
+  SOCKET_KINDS,
+  equipSlotsFor,
+  upgradeCost,
+  upgradeChance,
+  openSocketCost,
+  MAX_UPGRADE,
+  MAX_SOCKETS,
+  MOUNT_COST,
+};
+
 if (Store.isOffline) {
   Store.playOffline();
 }
diff --git a/src/store.ts b/src/store.ts
index 79e3b2e..67476c3 100644
--- a/src/store.ts
+++ b/src/store.ts
@@ -44,7 +44,18 @@ import { EventBus } from './libs/eventBus';
 import { Scalar } from './libs/babylon/exports';
 import { InventoryConstants } from './common/inventoryConstants';
 import { ItemGroups } from './common/objects/enum';
+import { rollExcOptions } from './offline/excellent';
 import { ItemsDatabase } from './common/itemsDatabase';
+import { applyExp, spendPoint } from './offline/offlineProgress';
+import {
+  clearSocket,
+  equipItem,
+  moveItem,
+  mountSocket,
+  openSocket,
+  unequipItem,
+  upgradeItem,
+} from './offline/itemActions';
 import { spawnPlayer } from './logic';
 
 const CONFIG_KEY = '_mu_key';
@@ -134,7 +145,10 @@ class PlayerData {
   sta = 10;
   eng = 10;
 
-  items: Item[] = new Array(
+  /* Khai (Item | null)[] cho đúng: mảng vốn được .fill(null) ngay dưới đây,
+     và cởi đồ thì ô phải trở lại null. Khai Item[] là mọi chỗ gán null đều
+     phải ép kiểu. */
+  items: (Item | null)[] = new Array(
     InventoryConstants.InventoryRows * InventoryConstants.RowSize +
       InventoryConstants.EquippableSlotsCount
   ).fill(null);
@@ -327,6 +341,11 @@ export const Store = new (class _Store {
 
   isOffline = location.href.includes('offline');
 
+  /* Hai công tắc của chế độ chơi đơn, giao diện điện thoại bật/tắt qua bridge.
+     Để ở Store cho OfflineCombatSystem và offlineProgress đọc chung một chỗ. */
+  autoAttack = false;
+  autoLoot = true;
+
   constructor() {
     makeObservable(this, {
       username: observable,
@@ -350,6 +369,119 @@ export const Store = new (class _Store {
     this.loadConfig();
   }
 
+  /* ------------------------------------------------------- chế độ chơi đơn
+     Bản chơi mạng đổi các chỉ số này bằng gói tin từ server. Chơi đơn không có
+     server nên OfflineCombatSystem gọi thẳng mấy hàm dưới đây. Tất cả bọc
+     runInAction để mobx báo cho giao diện gốc, còn HUD Aetherfall thì đọc theo
+     nhịp khung hình nên kiểu nào cũng thấy. */
+
+  setPlayerHp(hp: number) {
+    runInAction(() => {
+      this.playerData.currentHP = Math.max(0, Math.min(this.playerData.maxHP, hp));
+    });
+  }
+
+  setPlayerMp(mp: number) {
+    runInAction(() => {
+      this.playerData.currentMP = Math.max(0, Math.min(this.playerData.maxMP, mp));
+    });
+  }
+
+  gainZenOffline(amount: number) {
+    runInAction(() => {
+      this.playerData.money += amount;
+    });
+  }
+
+  /* ---------------------------------------------- thao tác với đồ (chơi đơn)
+     Việc thật nằm ở src/offline/itemActions.ts; ở đây chỉ bọc runInAction để
+     mobx thấy mảng items đổi và giao diện vẽ lại. */
+
+  moveItemOffline(from: number, to: number) {
+    let r: string | null = 'chưa chạy';
+    runInAction(() => {
+      r = moveItem(from, to);
+    });
+    return r;
+  }
+
+  equipItemOffline(bagSlot: number) {
+    let r: string | null = 'chưa chạy';
+    runInAction(() => {
+      r = equipItem(bagSlot);
+    });
+    return r;
+  }
+
+  unequipItemOffline(equipSlot: number) {
+    let r: string | null = 'chưa chạy';
+    runInAction(() => {
+      r = unequipItem(equipSlot);
+    });
+    return r;
+  }
+
+  upgradeItemOffline(slot: number) {
+    let r = { ok: false, reason: 'chưa chạy' };
+    runInAction(() => {
+      r = upgradeItem(slot);
+    });
+    return r;
+  }
+
+  openSocketOffline(slot: number) {
+    let r = { ok: false, reason: 'chưa chạy' };
+    runInAction(() => {
+      r = openSocket(slot);
+    });
+    return r;
+  }
+
+  mountSocketOffline(slot: number, index: number, kind: number) {
+    let r = { ok: false, reason: 'chưa chạy' };
+    runInAction(() => {
+      r = mountSocket(slot, index, kind);
+    });
+    return r;
+  }
+
+  clearSocketOffline(slot: number, index: number) {
+    let r = { ok: false, reason: 'chưa chạy' };
+    runInAction(() => {
+      r = clearSocket(slot, index);
+    });
+    return r;
+  }
+
+  /** Cộng một điểm chỉ số ở chế độ chơi đơn. */
+  spendPointOffline(stat: 'str' | 'agi' | 'sta' | 'eng') {
+    let ok = false;
+    runInAction(() => {
+      ok = spendPoint(stat);
+    });
+    return ok;
+  }
+
+  gainExpOffline(amount: number) {
+    runInAction(() => {
+      applyExp(amount);
+    });
+  }
+
+  putItemOffline(slot: number, item: Item) {
+    runInAction(() => {
+      this.playerData.items[slot] = item;
+    });
+  }
+
+  /** Chết thì hồi sinh đầy máu ngay tại chỗ — chơi đơn không có hình phạt. */
+  reviveOffline() {
+    runInAction(() => {
+      this.playerData.currentHP = this.playerData.maxHP;
+      this.playerData.currentMP = this.playerData.maxMP;
+    });
+  }
+
   playOffline() {
     if (!this.world) return;
 
@@ -409,14 +541,34 @@ export const Store = new (class _Store {
       isExcellent: false,
     };
 
-    const weapon = ItemsDatabase.getItem(3, 9); // bill spear
-    // const weapon = ItemsDatabase.getItem(1, 1); // small axe
+    /* Cánh cấp 3 của Dark Knight: Wing of Storm (nhóm 12 / số 36 → Wing08.glb).
+       Trong items.json cột lớp nhân vật của món này ghi MG, nhưng các cột
+       DW/DK/Elf/MG/DL của file ấy lệch một nấc — đã kiểm bằng Speed và các cột
+       Str/Agi, xem ghi chú trong src/offline/itemStats.ts. Theo đúng MU thì
+       cánh cấp 3 của DK là Wing of Storm. Đổi sang cánh khác chỉ cần sửa số
+       dưới đây; mọi cánh trong nhóm 12 đều nạp được. */
+    Store.playerData.items[InventoryConstants.WingsSlot] = {
+      group: ItemGroups.Wings,
+      num: 36,
+      lvl: 9,
+      isExcellent: false,
+    };
+
+    // Vũ khí: Sword of Archangel (nhóm 0 / số 19 → Archangelus.glb).
+    const weapon = ItemsDatabase.getItem(0, 19);
 
+    /* Kiếm khởi đầu là hàng Xuất sắc đủ SÁU dòng — để nhìn thấy ngay bảng
+       thông tin đầy đủ giống MU. Đồ rơi trong lúc chơi thì bốc ngẫu nhiên
+       1–5 dòng với tỉ lệ 3%, xem EXC_DROP_CHANCE bên offlineProgress.ts. */
+    /* Sword of Archangel là đồ THẦN (Divine) — tên hiện màu tím, và lớp phủ
+       phát sáng cũng tím, khớp với itemRank() bên src/offline/itemStats.ts. */
     Store.playerData.items[InventoryConstants.LeftHandSlot] = {
       group: weapon.Group,
       num: weapon.Index,
-      lvl: 9,
-      isExcellent: false,
+      lvl: 15,
+      isExcellent: true,
+      isDivine: true,
+      excOptions: 63,
     };
 
     Store.playerData.items[InventoryConstants.LastEquippableItemSlotIndex + 1] =
@@ -425,6 +577,7 @@ export const Store = new (class _Store {
         num: weapon.Index,
         lvl: 9,
         isExcellent: true,
+        excOptions: rollExcOptions(weapon.Group),
       };
 
     this.syncPlayerAppearance();
diff --git a/src/ui/components/itemIcon/index.tsx b/src/ui/components/itemIcon/index.tsx
index 709c9e1..ed75472 100644
--- a/src/ui/components/itemIcon/index.tsx
+++ b/src/ui/components/itemIcon/index.tsx
@@ -17,7 +17,7 @@ export const ItemIcon = memo(({ group, num, lvl, isExcellent }: Item) => {
 
   return (
     <img
-      src={`/items/item_${group}_${num}_${normalizedLvl}${
+      src={`${(typeof window !== 'undefined' && (window as any).MU_ASSETS?.itemsBase) || '/items/'}item_${group}_${num}_${normalizedLvl}${
         isExcellent ? '_e' : ''
       }.png`}
       className="item-icon"
